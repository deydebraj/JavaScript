//geneate a random decimal number in javaScript
function randomFunction(){
    return Math.random();
}
console.log(randomFunction());

////geneate a random whole number in javaScript
function randomFunction(){
    return Math.floor(Math.random()*10);        //between 1 to 10, but not 0
}
console.log(randomFunction());
